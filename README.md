# 2DDBD

**A top-down 2D pixel asymmetric horror game — a tribute to Dead by Daylight.**

[中文说明](README-zh.md) · [Planning document](Planning/Planning.md)

One killer. Four survivors. Five generators. Two gates. Repair the machines and escape — or hang them all.

---

## What this is

2DDBD ports the complete core loop of *Dead by Daylight* into a 2D top-down pixel world:

```
Survivors : repair 5 generators -> pull an exit gate lever -> escape
Killer    : down -> pick up -> hook -> sacrifice
```

Built from scratch with **Godot 4.5.1 + GDScript**. There is not a single imported asset in this
repository — every sprite is generated by `tools/gen_sprites.py`, every sound is synthesised by
`tools/gen_audio.py`, and every realm is produced deterministically by `MapGenerator`.
Clone it and you can reproduce the entire asset set byte for byte.

---

## Features

### Survivors
- Three movement gaits (run / walk / crouch), plus pallet drops, window vaults and lockers
- Repair, self-heal, heal others, unhook teammates, revive downed teammates
- **Skill checks**: a sweeping needle around a dial; Space inside the gold band is a perfect, grey is a good, and a miss makes the generator explode and gives away your position
- Four-step health ladder: healthy -> injured -> downed -> dying, with crawl movement and carry-wiggle
- Three hook stages, as in the original: a **first** hooking gives a 60 s rescue window and
  one 4 % self-unhook gamble; a **second** goes straight to the struggle phase, where the
  victim has to hold the interact key to fight the entity off; a **third** sacrifices
  immediately, with no rescue window. Repeat-hooking one post on the same person counts as
  one stage further along
- Running leaves **scratch marks**, bleeding leaves **blood**, crouching makes you harder to spot
- 4 items (med-kit / flashlight / toolbox / map) with 3 add-ons each
- 12 survivor perks, unlocked through the Bloodweb

### Killer — The Trapper
- Basic attack with wind-up and recovery, plus a charged lunge
- **Power: Bear Trap** — arm traps on the ground; a survivor who steps on one is held until they gamble on escaping
- Pick up, carry, and hook the nearest free hook
- Break dropped pallets, vault windows (much slower than survivors — which is exactly why window loops work)
- **Bloodlust**: chasing the same target for 15 / 25 / 35 s grants three speed tiers, reset by a pallet stun or by losing the target
- **Terror radius**: a 32 m heartbeat that gets faster and louder as he closes in
- 6 killer perks + 6 bear-trap add-ons, unlocked through the Bloodweb

### Realms
- 5 maps: Auto Haven Wreckers / MacMillan Estate / Coldwind Farm / Yamaoka Estate / Ormond Lake Mine
- **Structure-first generation.** The grid is divided into 16x16 plots and each one is
  assigned a blueprint -- main building, compound, outdoor gym (a U-shaped wall with a
  pallet in its mouth), shed, or an open yard of cover. The realm is *built*, not
  sprinkled: the alternative (random wall blobs on an open field) produces 85% bare
  floor, no corridors and nothing to loop around.
- **Each realm has its own layout recipe**, so the five play differently rather than
  being one place in five colour schemes. Wreckers is a scatter of sheds, the farm is
  mostly open ground, Ormond is the densest with buildings.
- The main building is a 13x13 ring with an interior loop: two circuits, three doorways
  and two windows, which is what makes a long chase possible.
- **Windows are only cut where both sides are confirmed walkable**, and pallets only
  land on genuine pinch points -- reachability is guaranteed by construction, not luck.
- Walls block movement **and line of sight** (light occluders baked into the TileSet)
- Ground texture is sampled from **value noise**, so patches of the same ground clump
  together instead of scattering per tile like television static
- Seed-driven and fully reproducible; the realm id is mixed into the seed, so one trial
  seed still yields five genuinely different layouts
- Minimap on `Tab` marking discovered generators, hooks, gates and the hatch

![A generated realm](docs/realms/macmillan.png)

*A generated realm. Dark = walls, yellow = generators, red = hooks, white = windows, blue = pallets, green = exit gates.*
- Ships a **map inspection tool**: `--dump-map` renders a realm to PNG and prints a
  readability report (openness, reachability, corridor ratio, generator spacing, hook
  coverage). This is how the layout was measured rather than guessed at.

### Red stain and vision occlusion
- **Red stain**: a pool of light on the ground at the killer's feet, in the direction he
  faces (3 m / 64 deg). Survivors watch it sweep across the floor to read which way he is
  committing, even through a wall -- one of the original's most important readability tools.
  It is a pool, not a searchlight: at 11 m it lit up half the screen and stopped saying
  anything about which way he was looking
- **Killer Instinct**: arrows pinned to the screen edges pointing at the hooked survivor,
  the open hatch, powered gates and any generator with progress on it, each labelled with
  its distance. Survivors deliberately do not get this -- not knowing is their whole game
- **Vision occlusion**: ambient darkness + wall light occluders + per-character point
  lights with shadows, so the far side of a wall genuinely falls dark; enemies behind a
  wall fade to translucent, and a survivor in a locker is not visible at all
- **Bloodlust** rises through three tiers at 15 / 25 / 35 s of chase; the stain deepens
  and a red vignette bleeds into the killer's screen edges. Pallet stuns or genuinely
  losing the target reset it (with a 16 m grace window)

### Systems

### In game

![Gameplay](docs/screenshot.png)

*Killer's view. The small red pool at his feet is the 3 m stain; the yellow arrow at the screen edge is Killer Instinct, with the target's distance.*

![Main menu](docs/menu.png)

*Menus. Latin text is the hand-made 5x7 bitmap font; Chinese falls back to a system face.*

### Bloodweb progression
- Every character owns a private ring of nodes: perks, items, add-ons and bloodpoint caches
- Higher tiers are wider and more expensive; taking 60% of a tier unlocks the next one
- The graph is generated deterministically from `(character, tier)`, so a save only
  stores the tier number and the node ids taken -- tiny, and identical on every machine
- Anything you unlock appears in the Loadout screen immediately; the rest stays locked

### Systems
- Finite state machines drive every behaviour (11 survivor states / 8 killer states)
- A\* grid pathfinding plus a decision layer, good enough for a full solo trial
- Dynamic audio: the heartbeat tracks the terror radius, chase music swaps in, 51 synthesised sounds
- Light and dark interface themes, English and Simplified Chinese, switchable live
- Smooth camera follow, slight chase zoom-out, screen shake on hits

---

## Controls

| Key | Survivor | Killer |
|---|---|---|
| `W A S D` | Move | Move |
| `Shift` | Walk (leaves no scratch marks) | — |
| `Ctrl` | Crouch | — |
| `Space` | Interact (hold) / skill check / struggle / wiggle | Pick up / hook |
| `E` | Interact (same as Space) | Same |
| `F` | Use item | — |
| Left click / `J` | — | Swing |
| Right click / `K` | — | Place / reclaim bear trap |
| `Tab` | Minimap | Minimap |
| `F11` | Toggle borderless fullscreen | Same |
| `Esc` | Pause menu | Pause menu |

---

## Running it

### 1. Just play (Windows)

```
build/2DDBD.exe
```

Double-click. No installer, no Godot required — every resource is packed inside the single exe.

### 2. Open it in the Godot editor

Requires **Godot 4.5.x** (developed on 4.5.1 stable).

```
1. Open this folder in Godot (select project.godot)
2. Let the first-time import finish
3. Press F5
```

### 3. Regenerate every asset from source

```bash
python tools/gen_sprites.py     # SVG sources + PNG runtime textures
python tools/gen_audio.py       # all 51 sounds (~75 s of audio)
```

Both scripts use **only the Python standard library** (PNG is hand-encoded with zlib), so there is
nothing to install. Generation is deterministic: the same script version always produces
byte-identical assets.

---

## Project layout

```
2DDBD/
├── project.godot              Godot project config (includes every input mapping)
├── icon.svg                   procedurally generated app icon
├── Planning/Planning.md       full design document (parity matrix / number tables / milestones)
├── export_presets.cfg         Windows export preset
├── src/
│   ├── autoload/              7 global singletons
│   │   ├── game_config.gd     single source of truth for every tunable number
│   │   ├── event_bus.gd       global signal hub (actors never touch the UI)
│   │   ├── save_data.gd       settings + bloodpoint wallet persistence
│   │   ├── locale.gd          EN/ZH string table (~200 entries)
│   │   ├── audio_director.gd  mixer buses, dynamic heartbeat, music scheduling
│   │   ├── net_bridge.gd      network backend abstraction
│   │   └── scene_router.gd    scene transitions with a fade
│   ├── core/                  enums, utilities, atlas slicer
│   ├── actors/
│   │   ├── character_base.gd  movement / animation / vision / scratch & blood / stun
│   │   ├── state_machine.gd   tiny reusable FSM
│   │   ├── survivor/          survivor + 11 state classes
│   │   └── killer/            killer + 8 state classes + the bear-trap power
│   ├── interactables/         generator / hook / pallet / window / locker / hatch / gate / chest / trap
│   ├── ai/                    survivor brain, killer brain, stuck watchdog
│   ├── systems/               match controller, skill check
│   ├── map/                   deterministic procedural level generation
│   ├── net/                   offline / ENet LAN / server-less WebRTC P2P
│   ├── ui/                    main menu, loadout, settings, multiplayer, tutorial, HUD, results, theme
│   └── data/                  JSON tables (killers / survivors / perks / items / maps)
├── assets/
│   ├── sprites/               runtime PNG textures (24 files, all generated)
│   ├── source_svg/            matching SVG vector sources (hand-editable)
│   └── audio/                 51 synthesised sounds (WAV 22.05 kHz mono)
├── tools/
│   ├── gen_sprites.py         pixel-art generator (dual SVG + PNG output)
│   └── gen_audio.py           sound synthesiser (stdlib only)
├── scenes/                    boot / main_menu / match / hud / results
└── build/                     export output
```

---

## Technical notes

**Why SVG for the art source?**
Godot 4 ships a ThorVG importer, so `.svg` files work directly as textures. Pixel art is written as
run-length `<rect>` spans, which keeps the vector sources small and lets a script recolour one
humanoid rig into four distinct survivors. The game itself ships **1:1 PNGs**, which sidesteps any
rasteriser anti-aliasing that would soften the pixel edges.

**The numbers are faithful.** The scale is `1 tile = 16 world px = 1 metre`, so Dead by Daylight's
real metre-per-second values go straight into the code: survivor 4.0 m/s, killer 4.6 m/s, carry 3.68,
attack recovery 2.76, terror radius 32 m, generator 80 s, exit lever 20 s, pallet break 2.6 s…

**Networking.** Three backends share one set of actor logic:
`NetOffline` (solo, AI fills the five seats), `NetEnet` (LAN, host-authoritative) and
`NetWebRTC` (server-less P2P — two players paste one block of SDP text at each other and get a
direct data channel, no signalling server involved).

---

## Known limitations

- The networking layer ships a working lobby, handshake and state-sync interface, but full combat
  synchronisation has not been play-tested with multiple machines. Play solo first.
- The AI completes the whole loop (repair, flee, rescue, heal, chase, hook) but its pallet-window
  mind games are still fairly basic.
- The UI uses a hand-made **5x7 bitmap font** (`tools/gen_font.py` builds an AngelCode
  BMFont from 95 hand-written glyphs); Chinese falls back to the system CJK face.
- Realm generation is deterministic: the same seed always builds the same realm, so
  multiplayer only needs to synchronise the seed and the realm id.
- Bloodweb costs and tier widths are pure functions, so the economy can be
  rebalanced later without invalidating anybody's save.

---

## Roadmap

See the eight milestones in [`Planning/Planning.md`](Planning/Planning.md) (M1 project foundation ->
M8 networking and release). Next up: a pixel bitmap font, more killers and powers, smarter loop AI,
and full multiplayer synchronisation.

---

## Legal

Non-commercial fan tribute and technical study. *Dead by Daylight* and its characters, realms and
marks are the property of **Behaviour Interactive Inc.** This repository contains **no original game
assets** — every graphic and sound is generated by the scripts included here and can be reproduced
from source.

Licensed under the **Available License** (see [LICENSE](LICENSE), text at
<https://license.kscm.top/available.md>).
