# 2026-09-26

## 2DDBD — 杀手三项强化（突刺 / 智商 / 幽灵隐身）

实现并验证用户三项需求（陌老师原话：AI太傻、加突刺攻击、幽灵隐身几乎没实现要重做）。

### 改动文件
- `src/autoload/game_config.gd`：突刺速度 `K_LUNGE=6.9`（1.5×基础4.6）；幽灵隐身常量全部改为忠于原版——`WRAITH_CLOAK_CLOAKED_SPEED=5.0`、`WRAITH_CLOAK_VIS_RANGE=20`、`WRAITH_CLOAK_SEMI_ALPHA=0.18`、`WRAITH_UNCLOAK_HASTE_MULT=1.5`、`WRAITH_UNCLOAK_HASTE_TIME=1.0`、`WRAITH_BELL_CLOAK_TIME=2.5`、`WRAITH_BELL_UNCLOAK_TIME=3.0`、`WRAITH_BELL_SLOW=0.35`。移除旧的 toggle/alpha/uncooldown 常量。
- `src/actors/killer/killer.gd`：重做钟（bell）为锁定+减速的 2.5s/3s 通道（`BellState`）；突刺=按住攻击键在挥击phase0以 `K_LUNGE` 前冲（`AttackState` quick vs lunge）；`base_speed()` 幽灵分支按钟/隐身/显身加速计算；`MoveState.update` 修复一个**真实 bug**——每帧 `try_pickup()` 会强制解除隐身的幽灵，导致幽灵永远无法保持隐身移动（已加 `not k.cloaked` 守卫，交互键路径仍正常）。
- `src/ai/killer_brain.gd`：挥击前先 `face_towards(target)`；近距（<2.6格）用 Quick，中距（<5.2格）用 Lunge；引导致系数 0.6。幽灵：游荡保持隐身，仅目标进入 4.5 格且 CHASE 才敲 3s 钟显身。
- `src/ai/survivor_brain.gd`：`_killer_danger()` 对隐身幽灵只在 20m 内视为威胁（ stealth 标志跳过红痕/注视规避）。
- `src/ui/hud.gd` + `locale.gd` + `killers.json`：钟进度条、文案、数据同步。
- `src/systems/match_controller.gd`：新增 3 个 headless 断言测试 `--test-wraith` / `--test-lunge` / `--test-ai`（遵循既有 `--test-*` 钩子模式），并修复 `--killer` / 测试标志真正覆盖随机杀手。

### 验证（headless 断言全 PASS）
调用姿势（关键）：`godot --headless --fixed-fps 60 --quit-after N res://scenes/match.tscn -- <args>` —— 直接传场景路径（不要走 `--boot-match`，否则会卡在菜单）。`--fixed-fps 60` 让帧数≈秒数，`--quit-after` 计数的是帧。
- `--as-killer --test-wraith`：钟2.5s进/3s出、隐身80px/s、显身110.4px/s(150%)、20m外alpha0内0.18 → RESULT PASS
- `--as-killer --test-lunge`：3.8m 处 quick 落空 / lunge 命中 / 前冲53px → PASS
- `--as-survivor --test-ai`：杀手挥击前面朝目标误差0.0°、0.5s内命中 → PASS
- 另跑 `--killer wraith/trapper --debug-match` 各 10s 冒烟，无脚本/解析错误。

### 构建 & 提交
- 已 `godot --headless --export-release "Windows Desktop" build/2DDBD.exe`（build/ 被 gitignore，exe 为重建产物）。
- 提交源码 9 文件并 push 到 `https://github.com/Developerprit/2DDBD`（用 PAT，HTTP/1.1 防 reset）。

### 注意
- `is_equal_approx()` 在 GDScript 4.5 只接受 2 参，带 epsilon 第三参会解析失败——改用 `absf(a-b) < tol`。
- headless 测试里要让杀手隔离：`killer.is_ai=false; killer.brain=null`，否则幽灵 AI 会在隐身瞬间自动敲钟显身，干扰测量。

---

## 2DDBD — 9 项修复/优化批次

用户原话（9 项）：修机太快/无法翻出柜子·校准·挣扎无按键提示/挂死模型还在/看不到挂数/技能系统优化/AI 无故蹲起/地图布局怪(缺板子·翻窗进墙·边角板子·板子缝隙)/攻击手感怪/血迹持续太久。

### 改动文件（12 个）
- `game_config.gd`：`GENERATOR_TIME 90->120s`（修机变慢/总进度增大）；`BLOOD_LIFETIME 25->12s`（血迹再缩短）。
- `ui/hud.gd`：`_sync_hooks()` 统计 HOOKED/DEAD 人数（挂数显示）；`_sync_perks()` 列技能；`_sync_prompt` 重写——校准显示「校准 + 按[互动]」、挣扎显示「连打/按」、柜子显示「按[互动]离开柜子」。
- `survivor.gd`：`DeadState.enter` 隐藏 `sprite.visible=false`（挂死不留尸体）；新增 `_perk_speed_mult/_perk_speed_expire` + `_grant_perk_speed()`，`_apply_passive_perks` 改为按时间戳持续施加速度 buff（**根因**：旧代码每帧把 `speed_scale` 重置为 1.0，把一次性 timer 给的 Sprint Burst/Adrenaline/Lithe 全吞了）；`_on_action_pressed` 新增 locker 分支（按互动离开柜子）；`_update_chase` 修复**缩进 bug**（`chase_timer=3.5` 后的 `if not in_chase:` 多缩进一级 → "Expected statement, found 'Indent'"）。
- `survivor_brain.gd`：去掉每 0.9s 随机蹲伏骰子（AI 无故蹲起），改为按距离/威胁选 RUN/WALK。
- `data/perks.json`：Lithe mods 键名 `speed_mult/duration/cooldown` -> `lithe_speed/lithe_time/lithe_cd`（避免与常驻 speed_mult 冲突）。
- `interactables/window_vault.gd`：`landing_point` 用 `nearest_open_cell` 吸附到最近可走格，且仅在落点仍在远侧时接受 → 修复「翻窗翻进墙里」。
- `interactables/pallet.gd`：DROPPED 碰撞体 30x14 -> 32x16 → 消除板子缝隙。
- `map/map_generator.gd`：`_cut_windows` 避开地图边角（x/y<=3 或 >=n-4 跳过）→ 修复边角窗；fallback `pallet_count` 14->16。
- `data/maps.json`：5 张图 `pallet_count` 12/14/12/13/12 -> **16/18/16/17/16**（`_add_outdoor_pallets` 会补足到该值，真正增加户外板子）→ 修复「该有板子的地方没有板子」。
- `locale.gd`：新增 `act.calibrate`/`hint.mash`/`hint.exit_locker`/`hud.hooked`/`hud.dead`/`hud.perks`；`hint.press` 改文案。
- `actors/killer/killer.gd`：`AttackState.physics` 快速攻击加短前冲（手感）。**关键坑**：上一轮改动引入了**两个连续 `else`**（非法），我先误改成 `elif`（`elif` 跟在 `else` 后同样非法）——正确做法是恢复单一 `if _phase==0: {...} else: {...}`，把「快速攻击前冲」放进 phase0 的**非 lunge 分支**，并把所有跨行三元 `a if c else b \` 改成显式 if（GDScript 4.5 对续行三元易解析失败）。

### 验证（headless，全 PASS，0 脚本错误）
`godot --headless res://scenes/boot.tscn -- --boot-match --test-<X>`（注意需 `--boot-match` 跳过标题屏；日志必须写**项目内**目录，Git Bash 的 `/tmp` 在工具调用间会失效）：
- vault / pallet / lunge / ai / wraith / kick / instinct / release 八个 `--test-*` 全 PASS。
- `--dump-map --map <5 张图>`：各 7 个 shack PASS，loops(板子+窗) 27~35，generators=5 hooks=12。
- `--debug-match` soak 20s：`script_errors=0`，macmillan `pallets=0/18`（由 14 提升），`gens=0/5`（修机确实变慢）。
- 导出 `build/2DDBD.exe`（98MB）成功，导出 exe 复跑 `--test-vault` PASS。

### 关键教训
- **"Killer 类无法解析" 会让整条链崩**：`killer.gd` 的语法错误会让 `match_controller.gd` 里所有 `var x := killer.xxx()` 报 "Cannot infer the type" —— 定位时要顺着「Could not parse global class "Killer"」找**源头**，不要被下游大量连锁报错误导。
- `godot --headless --import` **不能**兜底发现所有脚本错误（它不加载 match.tscn，故不解析 match_controller.gd）。必须用真实启动链路 `--boot-match` 跑一遍才算验证。
- 清 `.godot` 缓存无助于真实语法错误（先怀疑源码，再怀疑缓存）。

---

## 2DDBD — 5 项修复批次（翻越/说明页/按键/敲钟/隐身自我可见）

用户原话（5 项）：翻越距离太大 / 操作说明和制作信息没有任何内容 / 按空格才能校准但按空格才能修机 / 幽灵敲钟时不是站立不动而是移速降到 1.0 且松开右键可中断 / 幽灵隐身时自己看不到模型。

### 改动文件（9 个）
- `interactables/window_vault.gd`：`landing_point` 位移 `TILE*2.6 -> TILE*1.5`（跨越位移 4.20 -> 3.20 格；测试阈值 >1.0 仍满足）。
- `ui/main_menu.gd`：**根因** —— `_panel()` 里的 `VBoxContainer` 只设了横向 EXPAND，而 `RichTextLabel` 默认最小高度为 **0**，于是「只有标题+文本」的教程页/制作信息页被压成 0 高度 → 看起来「没有任何内容」（装备/设置页有固定高度的行所以正常）。修复：`r.fit_content = true` + `autowrap_mode` + `custom_minimum_size`。并重写 `_tutorial_text()` 用 `"\n".join(PackedStringArray([...]))`（避开脆弱的 `\` 续行），同步新键位与幽灵敲钟说明。
- `project.godot`：**根因 #3** —— `interact` 原本 = 空格(32)+E(69)，而校准(QTE)也读 `interact`；按住空格修机时 `is_action_just_pressed` 永不再触发 → 无法校准。改为 `interact` 仅 E(69)，新增 `skill_check` = 空格(32)。
- `actors/survivor/survivor.gd`：新增 `_on_skill_check_pressed()`；`_read_input` 分离 `skill_check` 与 `interact`；挣扎/扭动（连打类）改用 `skill_check`（空格），长按类仍用 `interact`（E）。
- `actors/killer/killer.gd`：
  - `_cloak_target_alpha()` **根因 #5** —— 本地玩家是杀手时场上无「非 AI 幸存者」，`nearest` 保持 1e9 → 返回 alpha 0.0 → 玩家把自己的模型看没了。修复：`if GameConfig.player_role == Enums.Team.KILLER: return 1.0`（测试默认 player_role=SURVIVOR，故 `--test-wraith` 距离断言不受影响）。
  - `base_speed()` 敲钟分支由 `K_RUN*WRAITH_BELL_SLOW` 改为 `WRAITH_BELL_SPEED`(=1.0 m/s)；`BellState.physics` 不再清零 `move_input`（敲钟可继续走动）；`BellState.update` 新增「松开 `use_power` 即取消」；新增 `_cancel_bell()`（不动隐身状态）。
- `autoload/game_config.gd`：新增 `WRAITH_BELL_SPEED := 1.0`。
- `autoload/locale.gd`：`hint.press`→「长按 [E]」、`hint.mash`→「连打 [空格]」、新增 `hint.skill_check`「按 [空格]」、`hint.exit_locker`→「按 [E] 离开柜子」、新增 `power.bell.cancelled`。
- `ui/hud.gd`：校准提示改用 `hint.skill_check`。
- `systems/match_controller.gd`：`--test-lunge` 去抖动 —— 原来只挑「第一处墙体射线畅通」的点，但 `blocked_by_wall` 只测 `LAYER_WORLD` 的**墙体瓦片**，**道具（箱子/岩石/车）有碰撞却测不到**，冲刺被道具削短（53.4px→19~35px）导致突刺假失败（实测 3/5~5/6 抖动）。改为：候选点 = 各角色位置 + 4 格步长扫全图可走格，逐个尝试**直到突刺真正命中**；快速攻击必须落空只断言一次。结果 **6/6 稳定**（位移 51.5~53.4）。

### 验证
- 主菜单启动 `--headless --quit-after 90`：0 错误（覆盖 tutorial/credits 构建）。
- vault/pallet/lunge/ai/wraith/kick/instinct/release **8 项全 PASS**，均 0 脚本错误。
- 输入探针（`_check/check_input.gd`，gitignored）：`interact`=69(E)、`skill_check`=32(空格)，各 1 绑定。
- 导出 `build/2DDBD.exe`（98MB）成功。提交 `f31d508`，推送 main。

---

## 2DDBD — 5 项特性批次（受伤加速/全遮挡+红光穿墙/幽灵AI敲钟/大门内区域/技能系统重构）

用户原话（5 项）：没有受伤加速(受伤时200%持续1秒) / 遮挡系统仍能看到对方只是看不清(要完全看不到,含自己队友,并且红光可穿透建筑) / 幽灵AI一直敲钟隐身现身 / 增加大门内区域 / 规范技能系统(每角色三个专属技能,血网60级后其他同阵营可用,通用技能也要通过血网获取)。

### 改动文件（11 个，+800/−158）
- `game_config.gd`：新增 `S_HIT_SPEED=2.0`/`S_HIT_SPEED_TIME=1.0`；`OCCLUDED_ALPHA 0.30 -> 0.0`。
- `survivor.gd`：`take_hit` 的 HEALTHY->INJURED 分支加 `_grant_perk_speed(2.0, 1.0)`；`setup()` **不再强塞** `personal_perk`（旧代码给玩家白送第 5 个技能、绕过血网）。
- `match_controller.gd`：`_update_occlusion` 删除「队友永远可见」例外（遮挡对称）；新增 `_filter_unlocked()`，开局过滤未解锁技能（防旧 last_loadout/改档绕过）；新增 `--test-perks` 与 `_run_perks_test`；`--test-ai` 去抖动；vault 断言放宽容差；debug 日志加 `bell=/cloak=`。
- `character_base.gd`：角色 `z_index = 2`（高于道具 0 与红光 1）。
- `red_stain.gd`：`z_as_relative=false` + `z_index=1`（**绝对**层级：高于墙瓦片 -50 与道具 0、低于角色 2）→ 红光可穿透建筑但被角色正确遮挡。
- `killer_brain.gd`：Wraith 敲钟重写——新增 `BELL_COOLDOWN=9.0`/`_bell_wait`/`_any_survivor_within()`；隐身仅在 `PATROL && seen==null && 14格内无人`；显形仅在 `CHASE && 8格内 && 有视线`；两次敲钟间强制冷却。新增静态计数器 `bell_rings`/`bell_cloaks`。
- `map_generator.gd`：新增 `GATE_ROOM_DEPTH=6`/`GATE_ROOM_HALF=2`；`_place_gates` 挖出真正的门内空场（沿中轴 6 格深、5 格宽，且**不挖穿边界墙** x=0/n-1）；评分循环同步放大。
- `map_dump.gd`：报告新增每扇门的 `vestibule depth/width` 度量。
- `bloodweb.gd`：`STARTER_PERKS` 删除 → `TEACHABLE_TIER=60`；新增 `exclusive_perks_for(char_id)`；`perk_pool()` 改为**只含通用技能**（owner==""）。
- `save_data.gd`：新增 `PERK_VERSION=2` + `migrate_to_three_exclusives()`（旧存档 unlocked 按新规则重建，保留 tier/taken；数据表未就绪则中止防误清）；`_grant_starters` 只发自己的 3 个专属；`unlocked_perk_ids` 追加大方阵营中 tier>=60 的角色的专属（教学）。
- `data/perks.json`：33 个技能全部加 `owner` 字段；新增 9 个技能（empathy/iron_will/calm_spirit/resurgence/tenacity/agitation/unnerving_presence/shadowborn/predator），**全部复用已实现的 mod 键**（aura_allies/crouch_mult/interact_mult/heal_speed/miss_recover/detect_radius/speed_mult/blood_life_mult）以保证真有效果。现状：5 逃生者 ×3 + 2 杀手 ×3 专属；通用 5(逃生)/7(杀手)。

### 验证（全 PASS，0 FAIL，0 脚本错误）
- `--test-perks`（新）：7 角色各 3 专属；fresh meg 只有自己 3 个；dwight@60 教学 3/3 给 meg；通用池无 owner。
- 其余 8 项 vault/pallet/lunge/ai/wraith/kick/instinct/release 全 PASS；`--test-ai` 与 `--test-vault` 复跑 6/5 次稳定。
- 5 张图 dump：gate vestibule depth=6/6 width=6~7/5 全 PASS，shacks 全 PASS。
- 幽灵 soak 40s：`bell=2/cloak=1`（旧版会一直敲），script_errors=0。
- 导出 exe 复跑 perks 测试 PASS + 菜单 0 错误。

### 关键教训
- **导出失败「重命名 build/2DDBD.tmp 失败」= 导出的游戏进程还在运行锁着 exe**。先 `taskkill -F -IM 2DDBD.exe` + `pkill -f Godot_v4.5.1` 再导出。
- **落点吸附到整格**会让「1.5 格」量化成正好 1.0 格，断言写 `> 1.0` 会假失败（16>16 为假）——要按瓦片量化放宽。
- **AI 行为测试极易被地图几何抖动**（`blocked_by_wall` 只测墙瓦片，测不到道具）：改成「多候选点重试直到真正成功」，--test-lunge 与 --test-ai 都这么修才稳定。

---

## 2DDBD — 8 项修复批次（突刺转向/敲钟时长/锁阵容/AI边走边敲/自动上肩/敲钟进度/E吞键/玩家角色随机）

### 改动文件（8 个，+188/−57）
- `game_config.gd`：`WRAITH_BELL_CLOAK_TIME 2.5->1.5`、`WRAITH_BELL_UNCLOAK_TIME 3.0->2.5`；新增 `K_LUNGE_TURN=3.0`（rad/s 突刺转向上限）。
- `killer.gd`：
  - `AttackState.physics` 突刺改为**每帧重读瞄准**并按 `K_LUNGE_TURN` 限速旋转 → 可曲线转向（原来锁定一次 `_lunge_dir`）。
  - `MoveState.update` **删除自动拾取**（走过濒死/倒地逃生者就瞬移上肩的真凶）→ 现在必须按 E。
  - `_interact_pressed` **不再调用 `_try_uncloak_for_action`**：隐身时按 E 只给提示 toast，敲钟只由力量键(RMB)触发（修复「E 把敲钟和交互互相吞掉」）。
- `killer_brain.gd`：`think()` 的早退列表去掉 `"bell"`，新增 `_bell_walk()` → AI 敲钟期间继续沿路径转向（受 base_speed 的 1.0 限制）；新增 `bell_move_frames` 计数器用于证明。`_any_survivor_within` 已在上一批加。
- `hud.gd`：**根因** `_local_survivor()` 在玩家是杀手时返回 null，导致 `_sync_prompt` 直接 return → 杀手**看不到任何提示**（含敲钟进度、抱起提示）。新增 `_sync_killer_prompt()` 专门处理杀手（敲钟时显示 `哀嚎之铃 敲钟中… 62%` + 进度条）。另外 `killer_panel` 用 `set_anchors_preset()+position=(8,-56)` 会被放到屏幕外（position 是相对父节点左上角的绝对量）→ 改用 `set_anchors_and_offsets_preset(PRESET_BOTTOM_LEFT, PRESET_MODE_MINSIZE, 8)`。
- `main_menu.gd`：`_role_buttons` 数组 + `_update_role_hint` 按 `SaveData.role_locked` 禁用两个阵营按钮并显示「首次进入后不可切换阵容」；`_start_match()` 首次开赛时写入 `role_locked/locked_role`。
- `save_data.gd`：新增 `role_locked`/`locked_role`，持久化在 `loadout` 段；`load_all` 里若已锁定则回写 `GameConfig.player_role`。
- `locale.gd`：新增 `loadout.role.locked`、`power.bell.need_uncloak`。
- `match_controller.gd`：`_spawn_actors` **玩家位不再随机**（`roster[0] = GameConfig.selected_survivor`；玩家当杀手时保留 `selected_killer`）；`--no-random-char` 真正生效（确定性阵容）；wraith 测试断言改为读常量（1.5/2.5）；debug 日志加 `bellwalk=`。

### 验证
- 菜单 0 错误；9 项测试全 PASS，0 FAIL，0 脚本错误。
- wraith：`cloaked ~1.52s`、`uncloaked ~2.52s`（跟随常量）。
- 45s 幽灵 soak：`bell=2/cloak=1/bellwalk=239` → AI 敲钟时确实在移动。
- `--survivor meg` → 首位 meg；`--survivor jake` → 首位 jake（玩家角色不再是随机）。
- 导出 exe（15:35）复跑 wraith 全 PASS。

### 关键教训（补充）
- **导出 `.tmp 重命名失败` 的真凶常常是"导出的游戏 exe 自己还在跑"**：`taskkill -F -IM <Game>.exe` 必须执行且要 `sleep` 等句柄释放；必要时**先导出成新文件名再 mv 覆盖**，最稳。
- **`Control.set_anchors_preset()` + `position=` 不等于"距某角 8px"**：`position` 是相对父节点左上角的量，配 BOTTOM 锚点会把控件推到屏幕外。正解 `set_anchors_and_offsets_preset(preset, PRESET_MODE_MINSIZE, margin)`。
- **"XX 看不到提示/进度" 先查 `_sync_*` 的早退分支**：本项目 `_sync_prompt` 对杀手直接 return，等于整个杀手侧提示都是死代码。

---

## 2DDBD — 3 项修复批次（AI 校准失误 / 杀手本能触发 / 力量仅右键）

用户原话：AI不会校准改为20%概率失误 / 杀手本能无法触发，只有(校准失误·12米内无视野翻越地形·特殊技能)才触发 / 让所有力量都只能使用右键触发。

### 改动（4 文件，+159/−8）
- `game_config.gd`：新增 `AI_SKILLCHECK_FAIL_CHANCE=0.20`、`KI_SKILL_REVEAL=3.0`、`KI_VAULT_RANGE=12.0`、`KI_VAULT_REVEAL=4.0`。
- `project.godot`：`use_power` **删除 K(75) 绑定**，只剩 `mouse2`（右键）→ 所有力量右键专属。
- `survivor.gd`：
  - `_ai_press_skill_check` 重写：AI 不读转盘，等指针到区域后**一次性掷骰**（20% 失误 → 发电机爆炸 / 否则 great）。旧代码永远精确命中金色区 → **AI 数学上不可能失误**。
  - 新增 `_reveal_to_killer(seconds)`（不受距离限制，直接取 `MatchController.instance.killer`）。
  - 校准 grade 0 分支 → `_reveal_to_killer(KI_SKILL_REVEAL)`（触发条件①）。
  - `notify_vaulted` 末尾 → 若与杀手距离 ≤ 12m **且** `blocked_by_wall` 为真 → `instinct_reveal(KI_VAULT_REVEAL)`（触发条件②）。
- `match_controller.gd`：新增 `--test-skillcheck`；`--test-instinct` 补两条断言（吹爆校准会揭示 / 隐蔽翻越会揭示 + 超距保持沉默）；**测试强制 SURVIVOR 视角**（见下）。

### 关键教训（重要）
- **headless 测试不能继承玩家真实存档**：上一批加了「首次进入锁定阵营」后，陌老师实际游玩时把阵营锁成了**杀手**（存档 `role_locked=true, locked_role=1`，还点了 2 个陷阱师血网节点），于是 `--test-ai` 报 "killer is local (no brain)"、`--test-wraith` 的 alpha 全变 1.0（因为 `_cloak_target_alpha` 判定 `player_role==KILLER` 就返回 1.0）。修法：任何 `--test-*` 运行都强制 `player_role = SURVIVOR`。**凡测试依赖持久化状态，就必须显式覆盖它。**
- 排查这类问题的第一步是**直接看存档文件**：路径 `%APPDATA%/Godot/app_userdata/<project name>/save.cfg`（项目名取 project.godot 的 `config/name`）。一看 `locked_role=1` 就全明白了。

### 验证
- 10 项测试全 PASS，0 FAIL，0 脚本错误（新增 skillcheck）。
- `--test-skillcheck`：800 次 → 失败 **19.4%**（目标 20%），great 645 / good 0。
- `--test-instinct`：吹爆校准 → 揭示 1 人 PASS；隐蔽翻越 → 揭示 PASS。
- 输入探针：`use_power=["mouse2"]`、`interact=["key69"]`、`skill_check=["key32"]`。
- 导出 exe（15:42）复跑 skillcheck/instinct 皆 PASS。

---

## 2DDBD — 幽灵隐身交互修正（+4% 交互速度）

用户原话：原作中隐身状态下是可以交互的，并且交互速度会加快 4%。（这是对上一批「隐身时按 E 只给提示」的**修正**——我当时误以为必须显形才能交互。）

### 改动（9 文件，+82/−33）
- `game_config.gd`：新增 `WRAITH_CLOAK_INTERACT_SPEED := 1.04`。
- `killer.gd`：
  - 新增 `interaction_speed_mult()`（隐身幽灵 → 1.04，否则 1.0）与 `scaled_interaction_time(base)`。
  - `pallet_break_time()` 末尾接 `scaled_interaction_time()`。
  - `HookingState.update` 的 1.1s/2.4s 改为 `/mult`。
  - `_interact_pressed` **删除**「cloaked 就 toast 返回」的门；`try_pickup` **删除** `_try_uncloak_for_action()` 调用；`_try_uncloak_for_action()` 整个函数删除（已无引用）。**攻击时仍需显形**（保留不变）。
- `window_vault.gd` / `pallet.gd` 的 `vault_time()`、`generator.gd` 的 `kick_time(actor)`：杀手分支改为 `基础时长 / actor.interaction_speed_mult()`（`has_method` 防御式调用）。
- `hud.gd`：删掉杀手提示里那句已失效的 `power.bell.need_uncloak`。
- `locale.gd`：删除 `power.bell.need_uncloak` 键。
- `main_menu.gd`：操作说明补充「隐身仍可交互（翻窗/踩板/踢机/挂钩），交互速度 +4%，但无法挥刀」。
- `match_controller.gd`：`--test-wraith` 新增两条断言——① 隐身倍率 1.04、翻窗 1.50s→1.44s；② **隐身状态下真的抱起成功**（`try_pickup` + `is_carrying`）。

### 验证
- 菜单 0 错误；10 项测试全 PASS，0 FAIL，0 脚本错误。
- wraith 测试连跑 3 次全 PASS，输出：`cloak interact mult=1.04 (open 1.00) vault 1.50s -> 1.44s`、`cloaked pickup=true carrying=true`。
- 导出 exe（15:49）复跑 wraith 全 PASS。

### 教训
- 「用户说的原作细节」优先于我自己的记忆：我上一批按自己的印象加了「显形才能交互」的门，属于**过度设计**。用户原话里说"原作是X"时，直接按 X 实现，不要加未经确认的限制。
- 导出时 `mv` 报 `Device or resource busy` 但 `tasklist` 已无相关进程 → 是**杀软/索引器对刚写完的 exe 的瞬时占用**，等几秒重试即可（这次第 2 次就成功了）。

---

## 2DDBD — 撤销阵营锁定 + 独立「阵营」页（用户纠正）

用户愤怒纠正：他要的是「把选阵营的界面单独做一个按钮」，我却把「除非初次进入否则无法再切换阵容」理解成**永久锁死**并实现了。我的错。

### 改动（3 文件，+149/−72）
- `save_data.gd`：**彻底删除** `role_locked` / `locked_role` 及加载回写、保存写入的代码。旧存档的这两个键直接不再读取 → 谁都不会再被锁死。
- 决定**不持久化所选阵营**：每次启动默认逃生者，菜单里随时改。这样存里**不存在任何可能被误认为锁的状态**。
- `main_menu.gd`：新增独立 **Side 页**（导航项「阵营」，Page 枚举追加 `SIDE` 且构建顺序放在最后以保持索引对齐）——两张阵营卡（选中高亮 + 金边）、各自说明、以及「当前：阵营 · 角色（可随时切换）」读数；点卡片即切换并刷新装备列表。Play 页只保留一行只读状态 + 一个「切换阵营」跳转按钮。
- 稳健性：`_show_page()` 现在对越界索引 `push_error`（此前会**静默显示错页**）；新增 `--menu-side` 调试入口用于 headless 验证该页。
- `locale.gd`：删除 `loadout.role.locked`，新增 `menu.side` / `side.*` 文案。

### 验证
- 菜单 0 错误；`--menu-side` 运行期输出 `pages=8 visible_index=7`（索引对齐、无越界）。
- 10 项测试全 PASS，0 FAIL，0 脚本错误。
- 导出 exe（15:56）复跑：菜单 0 错误、Side 页正常打开、wraith 套件 PASS。

### 教训（重要）
- **不要把「约束型」需求自作主张加强**。用户说「除非初次进入,否则无法再切换阵容」时，我应当**先确认**（它是「首次进入时选一次」还是「永久锁死」），而不是直接实现一个游戏内无法撤销的锁定。凡涉及**不可逆/破坏性的产品行为**（锁死、删除、覆盖），先问一句。
- **不要把游戏内无法自救的状态写进存档**。锁死阵营 = 用户只能删档，这是设计事故。若真要做类似限制，必须同时提供「游戏内重置」入口。
