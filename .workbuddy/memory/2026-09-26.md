# 2026-09-26

## 2DDBD — 杀手三项强化（突刺 / 智商 / 幽灵隐身）

实现并验证用户三项需求（陌老师原话：AI太傻、加突刺攻击、幽灵隐身几乎没实现要重做）。

### 改动文件
- `src/autoload/game_config.gd`：突刺速度 `K_LUNGE=6.9`（1.5×基础4.6）；幽灵隐身常量全部改为忠于原版——`WRAITH_CLOAK_CLOAKED_SPEED=5.0`、`WRAITH_CLOAK_VIS_RANGE=20`、`WRAITH_CLOAK_SEMI_ALPHA=0.18`、`WRAITH_UNCLOAK_HASTE_MULT=1.5`、`WRAITH_UNCLOAK_HASTE_TIME=1.0`、`WRAITH_BELL_CLOAK_TIME=2.5`、`WRAITH_BELL_UNCLOAK_TIME=3.0`、`WRAITH_BELL_SLOW=0.35`。移除旧的 toggle/alpha/uncooldown 常量。
- `src/actors/killer/killer.gd`：重做钟（bell）为锁定+减速的 2.5s/3s 通道（`BellState`）；突刺=按住攻击键在挥击phase0以 `K_LUNGE` 前冲（`AttackState` quick vs lunge）；`base_speed()` 幽灵分支按钟/隐身/显身加速计算；`MoveState.update` 修复一个**真实 bug**——每帧 `try_pickup()` 会强制解除隐身的幽灵，导致幽灵永远无法保持隐身移动（已加 `not k.cloaked` 守卫，交互键路径仍正常）。
- `src/ai/killer_brain.gd`：挥击前先 `face_towards(target)`；近距（<2.6格）用 Quick，中距（<5.2格）用 Lunge；引导致系数 0.6。幽灵：游荡保持隐身，仅目标进入 4.5 格且 CHASE 才敲 3s 钟显身。
- `src/ai/survivor_brain.gd`：`_killer_danger()` 对隐身幽灵只在 20m 内视为威胁（ stealth 标志跳过红痕/注视规避）。
- `src/ui/hud.gd` + `locale.gd` + `killers.json`：钟进度条、文案、数据同步。
- `src/systems/match_controller.gd`：新增 3 个 headless 断言测试 `--test-wraith` / `--test-lunge` / `--test-ai`（遵循既有 `--test-*` 钩子模式），并修复 `--killer` / 测试标志真正覆盖随机杀手。

### 验证（headless 断言全 PASS）
调用姿势（关键）：`godot --headless --fixed-fps 60 --quit-after N res://scenes/match.tscn -- <args>` —— 直接传场景路径（不要走 `--boot-match`，否则会卡在菜单）。`--fixed-fps 60` 让帧数≈秒数，`--quit-after` 计数的是帧。
- `--as-killer --test-wraith`：钟2.5s进/3s出、隐身80px/s、显身110.4px/s(150%)、20m外alpha0内0.18 → RESULT PASS
- `--as-killer --test-lunge`：3.8m 处 quick 落空 / lunge 命中 / 前冲53px → PASS
- `--as-survivor --test-ai`：杀手挥击前面朝目标误差0.0°、0.5s内命中 → PASS
- 另跑 `--killer wraith/trapper --debug-match` 各 10s 冒烟，无脚本/解析错误。

### 构建 & 提交
- 已 `godot --headless --export-release "Windows Desktop" build/2DDBD.exe`（build/ 被 gitignore，exe 为重建产物）。
- 提交源码 9 文件并 push 到 `https://github.com/Developerprit/2DDBD`（用 PAT，HTTP/1.1 防 reset）。

### 注意
- `is_equal_approx()` 在 GDScript 4.5 只接受 2 参，带 epsilon 第三参会解析失败——改用 `absf(a-b) < tol`。
- headless 测试里要让杀手隔离：`killer.is_ai=false; killer.brain=null`，否则幽灵 AI 会在隐身瞬间自动敲钟显身，干扰测量。

---

## 2DDBD — 9 项修复/优化批次

用户原话（9 项）：修机太快/无法翻出柜子·校准·挣扎无按键提示/挂死模型还在/看不到挂数/技能系统优化/AI 无故蹲起/地图布局怪(缺板子·翻窗进墙·边角板子·板子缝隙)/攻击手感怪/血迹持续太久。

### 改动文件（12 个）
- `game_config.gd`：`GENERATOR_TIME 90->120s`（修机变慢/总进度增大）；`BLOOD_LIFETIME 25->12s`（血迹再缩短）。
- `ui/hud.gd`：`_sync_hooks()` 统计 HOOKED/DEAD 人数（挂数显示）；`_sync_perks()` 列技能；`_sync_prompt` 重写——校准显示「校准 + 按[互动]」、挣扎显示「连打/按」、柜子显示「按[互动]离开柜子」。
- `survivor.gd`：`DeadState.enter` 隐藏 `sprite.visible=false`（挂死不留尸体）；新增 `_perk_speed_mult/_perk_speed_expire` + `_grant_perk_speed()`，`_apply_passive_perks` 改为按时间戳持续施加速度 buff（**根因**：旧代码每帧把 `speed_scale` 重置为 1.0，把一次性 timer 给的 Sprint Burst/Adrenaline/Lithe 全吞了）；`_on_action_pressed` 新增 locker 分支（按互动离开柜子）；`_update_chase` 修复**缩进 bug**（`chase_timer=3.5` 后的 `if not in_chase:` 多缩进一级 → "Expected statement, found 'Indent'"）。
- `survivor_brain.gd`：去掉每 0.9s 随机蹲伏骰子（AI 无故蹲起），改为按距离/威胁选 RUN/WALK。
- `data/perks.json`：Lithe mods 键名 `speed_mult/duration/cooldown` -> `lithe_speed/lithe_time/lithe_cd`（避免与常驻 speed_mult 冲突）。
- `interactables/window_vault.gd`：`landing_point` 用 `nearest_open_cell` 吸附到最近可走格，且仅在落点仍在远侧时接受 → 修复「翻窗翻进墙里」。
- `interactables/pallet.gd`：DROPPED 碰撞体 30x14 -> 32x16 → 消除板子缝隙。
- `map/map_generator.gd`：`_cut_windows` 避开地图边角（x/y<=3 或 >=n-4 跳过）→ 修复边角窗；fallback `pallet_count` 14->16。
- `data/maps.json`：5 张图 `pallet_count` 12/14/12/13/12 -> **16/18/16/17/16**（`_add_outdoor_pallets` 会补足到该值，真正增加户外板子）→ 修复「该有板子的地方没有板子」。
- `locale.gd`：新增 `act.calibrate`/`hint.mash`/`hint.exit_locker`/`hud.hooked`/`hud.dead`/`hud.perks`；`hint.press` 改文案。
- `actors/killer/killer.gd`：`AttackState.physics` 快速攻击加短前冲（手感）。**关键坑**：上一轮改动引入了**两个连续 `else`**（非法），我先误改成 `elif`（`elif` 跟在 `else` 后同样非法）——正确做法是恢复单一 `if _phase==0: {...} else: {...}`，把「快速攻击前冲」放进 phase0 的**非 lunge 分支**，并把所有跨行三元 `a if c else b \` 改成显式 if（GDScript 4.5 对续行三元易解析失败）。

### 验证（headless，全 PASS，0 脚本错误）
`godot --headless res://scenes/boot.tscn -- --boot-match --test-<X>`（注意需 `--boot-match` 跳过标题屏；日志必须写**项目内**目录，Git Bash 的 `/tmp` 在工具调用间会失效）：
- vault / pallet / lunge / ai / wraith / kick / instinct / release 八个 `--test-*` 全 PASS。
- `--dump-map --map <5 张图>`：各 7 个 shack PASS，loops(板子+窗) 27~35，generators=5 hooks=12。
- `--debug-match` soak 20s：`script_errors=0`，macmillan `pallets=0/18`（由 14 提升），`gens=0/5`（修机确实变慢）。
- 导出 `build/2DDBD.exe`（98MB）成功，导出 exe 复跑 `--test-vault` PASS。

### 关键教训
- **"Killer 类无法解析" 会让整条链崩**：`killer.gd` 的语法错误会让 `match_controller.gd` 里所有 `var x := killer.xxx()` 报 "Cannot infer the type" —— 定位时要顺着「Could not parse global class "Killer"」找**源头**，不要被下游大量连锁报错误导。
- `godot --headless --import` **不能**兜底发现所有脚本错误（它不加载 match.tscn，故不解析 match_controller.gd）。必须用真实启动链路 `--boot-match` 跑一遍才算验证。
- 清 `.godot` 缓存无助于真实语法错误（先怀疑源码，再怀疑缓存）。

---

## 2DDBD — 5 项修复批次（翻越/说明页/按键/敲钟/隐身自我可见）

用户原话（5 项）：翻越距离太大 / 操作说明和制作信息没有任何内容 / 按空格才能校准但按空格才能修机 / 幽灵敲钟时不是站立不动而是移速降到 1.0 且松开右键可中断 / 幽灵隐身时自己看不到模型。

### 改动文件（9 个）
- `interactables/window_vault.gd`：`landing_point` 位移 `TILE*2.6 -> TILE*1.5`（跨越位移 4.20 -> 3.20 格；测试阈值 >1.0 仍满足）。
- `ui/main_menu.gd`：**根因** —— `_panel()` 里的 `VBoxContainer` 只设了横向 EXPAND，而 `RichTextLabel` 默认最小高度为 **0**，于是「只有标题+文本」的教程页/制作信息页被压成 0 高度 → 看起来「没有任何内容」（装备/设置页有固定高度的行所以正常）。修复：`r.fit_content = true` + `autowrap_mode` + `custom_minimum_size`。并重写 `_tutorial_text()` 用 `"\n".join(PackedStringArray([...]))`（避开脆弱的 `\` 续行），同步新键位与幽灵敲钟说明。
- `project.godot`：**根因 #3** —— `interact` 原本 = 空格(32)+E(69)，而校准(QTE)也读 `interact`；按住空格修机时 `is_action_just_pressed` 永不再触发 → 无法校准。改为 `interact` 仅 E(69)，新增 `skill_check` = 空格(32)。
- `actors/survivor/survivor.gd`：新增 `_on_skill_check_pressed()`；`_read_input` 分离 `skill_check` 与 `interact`；挣扎/扭动（连打类）改用 `skill_check`（空格），长按类仍用 `interact`（E）。
- `actors/killer/killer.gd`：
  - `_cloak_target_alpha()` **根因 #5** —— 本地玩家是杀手时场上无「非 AI 幸存者」，`nearest` 保持 1e9 → 返回 alpha 0.0 → 玩家把自己的模型看没了。修复：`if GameConfig.player_role == Enums.Team.KILLER: return 1.0`（测试默认 player_role=SURVIVOR，故 `--test-wraith` 距离断言不受影响）。
  - `base_speed()` 敲钟分支由 `K_RUN*WRAITH_BELL_SLOW` 改为 `WRAITH_BELL_SPEED`(=1.0 m/s)；`BellState.physics` 不再清零 `move_input`（敲钟可继续走动）；`BellState.update` 新增「松开 `use_power` 即取消」；新增 `_cancel_bell()`（不动隐身状态）。
- `autoload/game_config.gd`：新增 `WRAITH_BELL_SPEED := 1.0`。
- `autoload/locale.gd`：`hint.press`→「长按 [E]」、`hint.mash`→「连打 [空格]」、新增 `hint.skill_check`「按 [空格]」、`hint.exit_locker`→「按 [E] 离开柜子」、新增 `power.bell.cancelled`。
- `ui/hud.gd`：校准提示改用 `hint.skill_check`。
- `systems/match_controller.gd`：`--test-lunge` 去抖动 —— 原来只挑「第一处墙体射线畅通」的点，但 `blocked_by_wall` 只测 `LAYER_WORLD` 的**墙体瓦片**，**道具（箱子/岩石/车）有碰撞却测不到**，冲刺被道具削短（53.4px→19~35px）导致突刺假失败（实测 3/5~5/6 抖动）。改为：候选点 = 各角色位置 + 4 格步长扫全图可走格，逐个尝试**直到突刺真正命中**；快速攻击必须落空只断言一次。结果 **6/6 稳定**（位移 51.5~53.4）。

### 验证
- 主菜单启动 `--headless --quit-after 90`：0 错误（覆盖 tutorial/credits 构建）。
- vault/pallet/lunge/ai/wraith/kick/instinct/release **8 项全 PASS**，均 0 脚本错误。
- 输入探针（`_check/check_input.gd`，gitignored）：`interact`=69(E)、`skill_check`=32(空格)，各 1 绑定。
- 导出 `build/2DDBD.exe`（98MB）成功。提交 `f31d508`，推送 main。
